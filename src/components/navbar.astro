<nav class="fixed bottom-0 left-0 w-full z-40 bg-gray-300 bg-opacity-70 backdrop-blur-md text-light-gray border border-solid
 border-slate rounded-tl-xl rounded-tr-xl sm:rounded-tl lg:absolute lg:bottom-auto lg:top-0 lg:left-auto lg:right-0
  lg:w-max sm:rounded-tr lg:rounded-bl lg:rounded-tl-none lg:px-5">
  <ul class="flex flex-wrap justify-center px-2.5 sm:gap-5 lg:gap-7 lg:px-5">
    <li class="list-none">
      <a href="/">
        <button class="py-5 px-1.5 text-light-gray text-xs sm:text-sm md:text-base md:font-medium hover:text-light transition ease-in-out duration-200">
          About
        </button>
      </a>
    </li>
    <li class="list-none">
      <a href="/portfolio/">
        <button class="py-5 px-1.5 text-light-gray text-xs sm:text-sm md:text-base md:font-medium hover:text-light transition ease-in-out duration-200">
          Portfolio
        </button>
      </a>
    </li>
  </ul>
</nav>


<script>
  function toggleActive(activate: string, deactivate: string) {
    const activateComponent = document.getElementById(activate);
    const deactivateComponent = document.getElementById(deactivate);
    if(activateComponent && deactivateComponent){
      activateComponent.classList.add('active');
      deactivateComponent.classList.remove('active');
    }
  }

  const aboutBtn = document.getElementById('aboutBtn');
  if (aboutBtn) {
    aboutBtn.addEventListener('click', function () {
      toggleActive('about', 'portfolio');
    });
  }

  const portfolioBtn = document.getElementById('portfolioBtn');
  if (portfolioBtn) {
    portfolioBtn.addEventListener('click', function () {
      toggleActive('portfolio', 'about');
    });
  }
</script>